# PRD.md — 「热门时事真伪辨别网站」产品需求文档

> 项目：热门时事真伪辨别网站（foxlings-project）
> 阶段：Day 4 · 撰写 PRD（五步工作流第 2 步：研究 → **PRD** → 设计 → 开发 → 发布迭代）
> 上游文档：`research.md`（Day 3 需求研究）
> 技术选型：本期不写（Day 5 做）；本文档不涉及任何具体框架和代码

---

## 一、项目概述

**一句话**：当一条热门消息刷屏时，给普通人一个快速、可靠、说人话的地方查"这事是真的假的"。

**解决的核心痛点**（来自 research.md）：
- 证伪门槛高：现有辟谣内容散落各平台，要么太专业、要么太零散，普通用户看完不知道该信谁；
- 耗时费力：自己交叉验证要开十几个搜索页、翻几篇长文，热点早过去了。

**产品定位**：不做"审判官"，做**"证据整理员"**（Snopes 启发）——每条热点给【结论标签 + 大白话依据 + 可点击信源 + 更新日期】，宁可说"尚无定论"，不硬编结论。

**目标用户**：**普通大众**。典型画像：
1. 刷微博/抖音/微信群时看到劲爆消息，第一反应"真的假的？"、但懒得自己验证的人；
2. 家里有爱转发养生文/震惊文长辈的年轻人——想找个能直接甩给爸妈看的链接。

## 二、页面 / 视图列表

MVP 共 **2 个页面、3 个视图**：

| 编号 | 页面/视图 | 是什么 | 为什么需要 |
|---|---|---|---|
| P1-V1 | 首页 · 热点卡片流（默认视图） | 今日精选 5–10 条热门时事，每条一张核查卡片 | 用户落地即看到"今天的热点哪些有问题" |
| P1-V2 | 首页 · 今日辟谣榜（切换视图） | 按日期展示"今天被戳穿的谣言"清单，按戳穿时间倒序 | 辟谣天然有时效性，"今天"是用户最关心的切片 |
| P2 | 详情页 | 单条热点的完整核查档案：追本溯源 + 多来源比对 | 想深究的用户能亲自验证，不用再开十个搜索页 |

不做的页面：搜索页、用户中心、评论页（见第六节）。

## 三、数据字段

每条热点记录包含以下字段（MVP 阶段人工维护，存在一个数据文件里）：

| 字段 | 说明 | 约束 |
|---|---|---|
| `id` | 唯一编号 | 必填，不重复 |
| `title` | 热点词条标题 | 必填，≤30 字 |
| `verdict` | 结论标签 | 必填，四选一：真 / 假 / 存疑 / 部分属实 |
| `summary` | 两三句大白话依据 | 必填，≤150 字 |
| `sources` | 信源列表 | 必填，≥2 条；每条含：来源名称、链接、发布方、发布日期 |
| `origin` | 追本溯源：这条消息最早出现在哪 | 必填 |
| `first_seen` | 消息最早流传日期 | 必填 |
| `updated_at` | 核查结论最后更新日期 | 必填 |
| `heat_note` | 热度说明（如"微博热搜前 10"） | 选填 |

> 说明：`sources` 至少 2 条是为支撑"多来源比对"；`origin` + `first_seen` 支撑"追本溯源"。

## 四、功能需求（每个功能 = 用户动作 + 可观察结果）

### F1 追本溯源（详情页）

**给用户什么**：一条谣言从哪来、怎么传开的时间线——把"道听途说"还原成"最早出处"。

- **用户动作 1**：在首页点任意一张热点卡片。
  **可观察结果**：跳转到该热点的详情页，页首显示标题、结论标签、最后更新日期。
- **用户动作 2**：在详情页查看「溯源时间线」区块。
  **可观察结果**：能看到按时间排列的 ≥2 个节点：消息最早出处（`origin`）→ 传播/发酵节点 → 当前核查状态，每个节点有日期。

### F2 多来源比对（详情页）

**给用户什么**：不止一个信源，且能看出信源之间是否互相印证——这是"证据整理员"的核心。

- **用户动作 1**：在详情页查看「信源比对」区块。
  **可观察结果**：看到 ≥2 条信源卡片，每张卡有：来源名称（可点击链接）、发布方、发布日期、该信源说了什么（一句话摘要）。
- **用户动作 2**：点击任意信源链接。
  **可观察结果**：新标签页打开原始出处，用户可亲自验证。
- **用户动作 3**：查看信源下方的「比对结论」一行字。
  **可观察结果**：看到信源之间关系的明确说明，三选一：相互印证 / 存在矛盾（并说明矛盾点）/ 信源不足（标注"待核实"）。

### F3 今日辟谣榜（首页切换视图）

**给用户什么**：今天被戳穿的谣言清单——最直接回应"我今天刷到的这条是不是已经被辟了"。

- **用户动作 1**：在首页点击「今日辟谣榜」标签切换视图。
  **可观察结果**：卡片流切换为榜单列表，显示当日 `updated_at` 为今天且 `verdict` 为"假/部分属实"的条目，按更新时间倒序。
- **用户动作 2**：榜单为空时查看页面。
  **可观察结果**：看到明确的空状态提示文案（如"今天暂无新增辟谣"），不是白屏。
- **用户动作 3**：点击榜单中任意条目。
  **可观察结果**：进入该条目的详情页。

### F0 结论标签体系（贯穿所有页面）

沿用 research.md 从 Snopes 学到的克制原则，四档标签：**真 / 假 / 存疑 / 部分属实**。

- **用户动作**：查看任何卡片或详情页。
  **可观察结果**：每条热点都有且仅有一个结论标签，"存疑"是合法且被允许的答案。

## 五、验收标准（可逐条检查）

**P1 首页**
- [ ] A1 打开首页，能看到 5–10 张热点卡片，每张卡片含：标题、结论标签、一句摘要、更新日期
- [ ] A2 每张卡片的结论标签必属于四档之一：真 / 假 / 存疑 / 部分属实
- [ ] A3 点击「今日辟谣榜」标签，视图切换成功且不白屏；无当日数据时显示空状态提示
- [ ] A4 今日辟谣榜内的条目，其结论标签均为"假"或"部分属实"，且更新日期为当日

**P2 详情页**
- [ ] A5 首页点击任意卡片 → 进入对应详情页，标题与所点卡片一致
- [ ] A6 详情页有「溯源时间线」：≥2 个按时间排列的节点，最早节点标注消息出处
- [ ] A7 详情页有「信源比对」：≥2 条信源卡片，每条有可点击链接
- [ ] A8 点击信源链接 → 新标签页打开原始出处
- [ ] A9 信源下方有「比对结论」一行字，三选一：相互印证 / 存在矛盾（含说明）/ 信源不足（标注待核实）

**数据与内容**
- [ ] A10 每条数据的 `sources` 字段均 ≥2 条，缺任何一条即不通过
- [ ] A11 所有"真/假"结论的条目，其信源中至少有一条来自权威渠道（指：政府/官方机构网站、官方通报原文、中央级媒体如新华社/央视/人民日报、或地方党媒——点到链接即可核对）
- [ ] A12 页面顶部能看到项目名「热门时事真伪辨别」——用户能一眼确认"这是哪个网站"

**验收方式**：全部由人眼在浏览器中逐条核对（对应 Day 7 MVP 验收）。

## 六、本期不做（后续清单）

从 research.md 平移，PRD 阶段逐条确认仍然不做：

1. ❌ 用户提交待核查信息（需账号系统 + 审核流，复杂度爆炸）
2. ❌ AI 自动核查 / 自动抓取热搜（爬虫 + 后端，28 天做不稳，法律风险不明）——**因此 F1/F2 的溯源与比对均为人工整理，不做自动化**
3. ❌ 可信度评分算法（较真AI 做了三年才敢上，本期先人工标注）
4. ❌ 用户注册登录、评论、点赞等社区功能
5. ❌ 手机 APP / 微信小程序（网页做好响应式即可）
6. ❌ 视频、图片鉴伪（AI 鉴伪是专业领域）
7. ❌ 英文版 / 多语言
8. ❌ 搜索功能、历史辟谣归档（今日辟谣榜只看"今天"，往期先不做）

## 七、成功指标（MVP 上线后 2 周内观察）

- 内容指标：每日更新 5–10 条，今日辟谣榜每个自然日非空 ≥5 天
- 质量指标：零次"结论翻车"（发出的结论标签被权威信源推翻）
- 用户指标：能把链接直接甩给家人朋友看（主观确认即可，本期不装统计代码）

## 八、待核实

- 热点词条人工筛选的版权/资质边界（research.md 遗留，Day 5 前问老师）
- 「今日辟谣榜」的数据维护节奏：由项目主人每日手动更新，若某天没更新，榜单按"最近更新日"展示并标注日期

## 九、参考资料

沿用 `research.md` 第八节全部 10 条来源，本文档不新增外部事实引用。
