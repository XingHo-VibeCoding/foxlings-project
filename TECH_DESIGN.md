# TECH_DESIGN.md — 「热门时事真伪辨别网站」技术设计文档

> 项目：热门时事真伪辨别网站（foxlings-project）
> 阶段：Day 5 · 技术路线生成（五步工作流第 3 步前置：研究 → PRD → **技术设计** → 设计 → 开发）
> 上游文档：`PRD.md`（Day 4，功能与验收标准的唯一来源）；`research.md`（Day 3）
> 本文不写代码，只定"用什么、为什么、数据怎么流"。

---

## 一、技术路线（一句话版）

> **前端原生 HTML + CSS + JavaScript 三件套，数据用随仓库版本管理的 `data.json`，无后端、无数据库，部署到 GitHub Pages 静态托管。**

## 二、方案比较（3 套，每套含前端 / 后端 / 数据库 / 部署）

### 方案 A：纯静态三件套（✅ 已拍板，2026-09-23）

| 层 | 选型 | 理由 |
|---|---|---|
| 前端 | 原生 HTML + CSS + 原生 JS | PRD 全部交互（四档切换、本地过滤检索、悬停动效）浏览器端即可完成；零学习成本前置，代码即所写即所见 |
| 后端 | **无** | PRD 无账号、无提交、无评论——没有一件活需要后厨；砍掉 = 零成本、零运维、少一整类安全漏洞 |
| 数据库 | **无**（`data.json` 代替） | 日更 5–10 条人工数据，JSON 文件足够；随 Git 版本管理，改错可回滚 |
| 部署 | GitHub Pages | 免费且已就位（仓库现成）；推送即上线，符合"零成本零运维" |

**取舍（诚实版）**：牺牲了"多页面共用导航栏不用复制粘贴"的工程便利（原生方案里公共部分需 JS 注入或少量重复）；牺牲了服务端能力（未来若要搜索排序复杂化需重做）。换来的是：28 天内不学框架、不配环境、不付服务器钱。

### 方案 B：Vite + Vue 3 + CloudBase 静态网站托管

| 层 | 选型 | 说明 |
|---|---|---|
| 前端 | Vite 构建 + Vue 3 | 组件化好，页面共用部分（导航/卡片）只写一次 |
| 后端 | 仍无（PRD 决定） | 框架不改变"无后端"这个事实 |
| 数据库 | 仍 `data.json` | 同上 |
| 部署 | 腾讯 CloudBase 静态托管 | 国内访问通常更稳，但需实名开通、配置部署流程 |

**不选的理由**：组件化的收益在"页面多、逻辑重"时才显现，我们 MVP 仅 2 页面 3 视图；代价却是实打实的——多学一层构建工具链（node_modules、打包、路由），新手排错难度翻倍。**留作 Day 7 后如页面膨胀到 5+ 时的升级路径。**

### 方案 C：React/Vite + CloudBase Node.js 云函数 + CloudBase PostgreSQL + CloudBase 托管（示范重装备路线）

| 层 | 选型 | 说明 |
|---|---|---|
| 前端 | React + Vite | 生态最大，但对零基础是三套新概念（组件、状态、hooks） |
| 后端 | Node.js 云函数 | 给"动态活"准备的：登录、提交、审核流——PRD 明确本期不做 |
| 数据库 | CloudBase PostgreSQL | 给"大量并发读写"准备的：我们的数据一天 5–10 条 |
| 部署 | CloudBase 静态托管 | 需实名开通与服务端配置 |

**不选的理由**：四层里两层（云函数、PostgreSQL）在为 PRD 第六节明确砍掉的功能服务——**给不存在的需求付真实的复杂度**。28 天工期里，学 React + 学云函数 + 学数据库的时间挤占的是 Day 7 MVP 本体。这不是"路线不好"，是"路线和需求不匹配"。

### 对比一览

| 维度 | A 纯静态 ✅ | B Vite+Vue | C 全家桶 |
|---|---|---|---|
| 上手难度 | ★☆☆☆☆ | ★★★☆☆ | ★★★★★ |
| 满足 PRD 全部验收标准 | 能 | 能 | 能（大材小用） |
| 新增要学的东西 | 0 | 构建链+框架 | 框架+云函数+数据库 |
| 月成本 | 0 | 0~低 | 低（超量计费） |
| 未来升级路径 | →B →C 均可平滑迁 | →C | — |

## 三、项目结构（规划，Day 6 设计后微调）

```
foxlings-project/
├── index.html          # 首页（含卡片流 V1 + 辟谣榜 V2 两个视图）
├── detail.html         # 详情页 P2（溯源时间线 + 信源比对）
├── css/
│   └── style.css       # 全站样式与动效规范（Day 6 设计产出）
├── js/
│   ├── data.js         # 数据加载与校验（读 data.json）
│   ├── home.js         # 首页逻辑：卡片渲染、档位切换、检索
│   └── detail.js       # 详情页逻辑：时间线渲染、信源卡片
├── data/
│   └── data.json       # 唯一数据源（PRD 第三节 9 字段）
├── docs/               # 本文档与 PRD、research.md 的最终归宿（迁移日整理）
├── AGENTS.md / .gitignore / README.md
└── TECH_DESIGN.md      # 本文档
```

原则：**依赖零原则**——不引入任何第三方库/框架/包管理器；浏览器打开 `index.html` 即应可运行（数据加载除外，见第七节）。

## 四、数据模型（`data.json` 结构）

字段与 PRD 第三节一一对应（字段名沿用，不另造）：

```json
{
  "items": [
    {
      "id": "20260923-01",
      "title": "（≤30 字热点词条标题）",
      "verdict": "假",
      "summary": "（≤150 字大白话依据）",
      "sources": [
        { "name": "来源名称", "url": "https://…", "publisher": "发布方", "date": "2026-09-20" }
      ],
      "origin": "（消息最早出处）",
      "first_seen": "2026-09-20",
      "updated_at": "2026-09-23",
      "heat_note": "微博热搜前 10"
    }
  ]
}
```

约定：
- `verdict` 四选一：`真 / 假 / 存疑 / 部分属实`（PRD F0）；
- `sources` ≥2 条，`verdict` 为"真/假"时至少 1 条权威渠道（PRD A11）；
- `origin` 允许「未能溯源」值（附已知最早流传信息）——工具边界诚实标注，未来由人工渠道（互助溯源论坛，长期方向）补位；
- **日/周/月/年四个榜不是四份数据**，由 `updated_at` 在前端实时过滤得出——数据只存一份，杜绝四份不同步的灾难。

## 五、API 列表

**MVP 阶段：0 个后端 API。** 诚实的"API 清单"只有一条：

| 调用 | 方向 | 说明 |
|---|---|---|
| `GET data/data.json` | 浏览器 → GitHub Pages | 页面加载时一次拉取全量数据（当前量级 <100KB，一次全拉比分页快且简单） |

**预留（不在本期实现，仅立此存照）**：未来若迁移到方案 C，需要的 API 大致为 `GET /api/items`（列表+时间档过滤）、`GET /api/items/:id`（详情）、`POST /api/search`（服务端检索）。现在不写，是为了不伺候不存在的调用方。

## 六、前后端数据流

**图形版**：见仓库根目录 `data-flow.svg`（GitHub 网页可直接预览）。

**文字版**：

```
数据流入（每日人工）：
核查对象（全网公开流传内容：帖子/短视频/群聊截图等）
  ＋ 佐证信源（权威渠道：官方通报/持牌媒体）
→ 人工核查整理 → data.json → git commit + push → GitHub Pages

用户访问（只读）：
用户浏览器 → 打开 index.html → js/data.js 拉取 data.json →
  ├─ 校验（字段齐全？verdict 合法？）→ 通过 → 渲染卡片流 V1
  └─ 用户交互（切日/周/月/年档、输入检索词）→ 全部在浏览器内过滤已加载数据 → 重渲染 → 用户看到结果

数据流出：无（本期网站不收集任何用户数据，无上传通道）
```

**掌握题一句话**：数据从全网公开流传内容（核查对象）与权威信源（佐证证据）中来，经人工整理进 `data.json` 随 Git 上到 GitHub Pages，在用户浏览器里被读取、过滤、渲染——全程只进不出、只读不写。

## 七、错误处理

| 场景 | 处理方式 | 对应验收 |
|---|---|---|
| `data.json` 加载失败（网络抖动） | 页面显示重试按钮与友好提示，不白屏 | 补充于 A3 精神 |
| JSON 格式坏掉 / 字段缺失 | `data.js` 校验后**跳过坏条目**并在控制台警告，页面照常渲染其余条目 | — |
| `verdict` 值不在四档内 | 视为坏条目，同上 | A2 |
| 榜档/搜索结果为空 | 显示空状态文案（"本时间段暂无匹配的辟谣记录"） | A3 / A4b |
| 信源链接失效 | 本期不做自动检测；用户发现后改 `data.json` 下次推送即修复（Git 留痕） | A8 运营侧 |

## 八、环境变量

**MVP 阶段：0 个环境变量。** 这不是省事，是架构事实：没有服务器就没有服务器密钥。

- `.env` 文件本期**不存在也不需要**；`.gitignore` 中的 `.env` 规则（Day 2 已配）是为将来预留的——第 23 天若接入数据库连接串，写进 `.env` 并继续保持不进仓库；
- 唯一需要变的"配置"是 GitHub Pages 的仓库名，属于部署设置，不涉及密钥。

## 九、迁移注意事项（何时、怎么换路线）

**触发条件**（满足其一才考虑迁移，别为迁移而迁移）：
1. 页面膨胀到 5 个以上，公共部分复制粘贴开始失控 → 升级方案 B；
2. 出现真实的用户提交/账号需求（即 PRD 第六节第 1 条解禁）→ 升级方案 C。

**方案变化时受影响的文件（影响面表，余力加练版）**：

| 文件 | 升级到 B（Vite+Vue） | 升级到 C（+云函数+PG） |
|---|---|---|
| `index.html` / `detail.html` | 改写为组件模板 | 同 B |
| `css/style.css` | **基本不动**（设计资产平移） | 基本不动 |
| `js/*.js` | 逻辑改写为组件 | 逻辑改写 + API 调用层 |
| `data/data.json` | **不动** | 结构平移进 PostgreSQL 表（`items` 表，字段一一对应），JSON 退役为导入脚本输入 |
| `TECH_DESIGN.md` | 改技术路线行 | 重写第五、六、八节 |
| `.env` | 仍不需要 | **新增**：数据库连接串（不进仓库） |

**迁移纪律**：迁移当天起 `data.json` 与数据库**只许一处为准**，禁止双写过渡期超过一天——两处真相是数据不一致之母。

## 十、临时假设（信息不足处，标"假设"待核实）

1. 「假设」GitHub Pages 在国内的访问稳定性可接受（research.md 遗留待核实项，Day 7 部署后实测；不稳则评估方案 B 的 CloudBase 托管做镜像）；
2. 「假设」`data.json` 全量单文件在 28 天内不会超过几百 KB（按日更 10 条估算，一年约 1MB，仍可全量加载）；
3. 「假设」`docs/` 目录整理（PRD/research/TECH_DESIGN 归档）放到 Day 6 或更晚做，今天不动现有文件位置。

## 十一、参考资料

无新增外部引用；选型判断依据均来自 `PRD.md` 与 `research.md`。
